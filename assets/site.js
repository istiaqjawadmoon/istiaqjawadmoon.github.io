const b=document.querySelector('.menu'),n=document.querySelector('.navlinks');if(b&&n)b.onclick=()=>n.classList.toggle('open');document.querySelectorAll('[data-year]').forEach(e=>e.textContent=new Date().getFullYear());fetch('stories.json').then(r=>r.json()).then(stories=>{const cards=document.querySelectorAll('.story-card');stories.slice(0,cards.length).forEach((story,i)=>{if(story.image){cards[i].style.backgroundImage=`url("${story.image}")`;cards[i].style.backgroundSize='cover';cards[i].style.backgroundPosition='center';cards[i].style.backgroundRepeat='no-repeat'}})}).catch(e=>console.error('Could not load story images:',e));
